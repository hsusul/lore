{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal core permissions for the main window. No updater, no filesystem, no shell, no network plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
